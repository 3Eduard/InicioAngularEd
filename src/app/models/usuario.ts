export interface usuario
{
  id?: number;         //? este signo indica que puede o no puede estar el valor
  usuario:string;
  cont: string;
  profile:string
  create_us?: string;
  update_us?: string;
}
